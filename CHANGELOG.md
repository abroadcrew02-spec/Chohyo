# Changelog

このプロジェクトの特筆すべき変更を記録する。書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、バージョニングは [Semantic Versioning](https://semver.org/lang/ja/) に従う。

## [Unreleased]

全体品質レビュー5巡目（#54〜#62）の反映。誤った値を無言で出す経路の遮断と、テンプレート保存で除外領域が失われる事故への対策が中心。続けて出力列制御 MVP（#66）を追加——テンプレートの座標定義を壊さずに、どの欄を出力するか・どの順で出すかを画面から変えられる。

7巡目（#69・2026-09-02）では、無言で失われる情報（範囲外の除外領域・歪んだ入力・様式不一致の終了コード）と、GUI から広げられる読み取り・送信の範囲を塞いだ。個人情報の削除手段も出力ファイルまで広げている（`purge --include-output`）。

テスト件数の正本は [docs/design/chouhyo-ocr/03_test_requirements.md](docs/design/chouhyo-ocr/03_test_requirements.md) §3（回帰ゲート基準値）。本書と README には件数を書かない——過去エントリに残る件数はその時点の記録であって現在値ではない (#53 L-1)。

### Added

- **テンプレート編集で帳票を開いたときの手順を変えた（枠判定の自動化・段 (b)+(t) の接続）**: 帳票の画像か PDF を開くと、前に使ったテンプレートがあればそれを自動で適用する。無ければテンプレートは適用せず、その紙の枠候補を自動で作り、画面の上に「テンプレートを選ぶ」というカードを出す——保存済みのテンプレートを使うか、候補から新しく作るかの二択で、決めるとカードは1行の帯に畳まれる。候補は**必ず空のテンプレートの上**で作るので、出荷テンプレートの欄が混ざることはない。様式が合わない紙の黄色い案内は、テンプレートを適用しているときだけ出す（候補を作る側では、比べる相手のテンプレートを捨てているため）。「この紙用に新しいテンプレートを作る」を押したときも、続けて候補を作るところまで進む。一覧のボタン名は「開く」から「このテンプレートを使う」に変え、推奨の印は★からバッジにした。テンプレートを適用したときと候補で作り直したときの結果は、上部の帯1本にまとめて読み上げる（同じ操作で複数の案内が同時に鳴ると、どれが今の変化か分からなくなるため）。カードの中の補足（欄数・更新日時・スコア）はテンプレート名より小さく薄い字にした。
  - 適用したテンプレートは `config.json` の `last_applied_template` に覚える（値は `""`・`"shipped"`・`"user:<名前>"` の3形だけで、**場所（絶対パス）は保存しない**）。読み取り画面で選ぶテンプレート（`last_template`）とは別のキーで、自動で適用しただけのときは読み取り画面の選択を書き換えない。覚えていたテンプレートが消えていたら、黙って出荷テンプレートに切り替えず、記憶を捨てて候補を作り直す。値が壊れていても読み取りは止まらない（`last_template` と同じく「記憶なし」として扱う）
  - 開くたびに候補を作るのに数秒かかる。以前の動きに戻したいときは `config.json` に `auto_detect_frames_on_open: false` を書く（既定は `true`。`snap_blocks` と同じく画面の設定には出さない）。書くと、開いたときの動きは前回のテンプレートの自動適用も含めて以前と同じになる
  - 枠候補を作るときは、画面に出ていないテンプレートを裏で使わない（`detect-frames` には画面が明示したときだけテンプレートを渡す）。これまでは読み取り画面で選んでいるテンプレートが自動で渡っており、その除外範囲（出荷テンプレートでは9か所・うち綴じ穴の帯2本は紙のほぼ全高）を白く塗り潰した画像から線を探していた——空のテンプレートで開いたはずの紙で、罫線が黙って消えることがあった
  - 候補を作っている数秒の間は「帳票を開く」「テンプレートを開く」「このテンプレートを使う」を押せないようにした。押せた頃は、あとから届いた候補が適用済みのテンプレートの上に重なって出て、まとめて採用すると枠が重なった。ファイルからテンプレートを開いたときは、上部の帯が前のテンプレートの名前を出したままにならないようにした
- **読み取ったデータを画面から削除できるようにした（#52 M-11・#69 残置5）**: 実行画面の下に「読み取ったデータを削除」を置いた。押すと二段の確認が出る——1段目で何が消えて何が残るか（消えるのは読み取りの途中経過、残るのは認証キー・テンプレート・設定）と、出力した Excel・CSV も消すかの選択、2段目で最終確認。読み取り中は押せない。終わると削除できた件数・削除できなかった件数・対象外として残したファイル数が「実行時のお知らせ」に出る（場所そのものは表示しない）。これまで削除手段はコマンドだけで、画面からは中間データ（記入内容がそのまま入る）を消せなかったため、実質的に無期限で溜まっていた。要件 §6.3「削除は明示操作のみ」は、自動削除をしないことと、この二段確認を通らなければ何も消えないことで満たしている
- **溢れの疑いを数える点検コマンド（`diag-overflow`・#63）**: 郵便番号のように桁数の決まっている欄で、枠に入りきらなかった文字が右隣へはみ出した疑いが何件あるかを数える。**数えるだけで何も直さない**——この型（主枠に部分的に記入され、残りが隣の欄へ入る）は今の検知網のどれにも掛からず、誤った値が「正常」の顔で出る。検知の線引きを決める前に実データでの頻度を測るための材料で、線引き自体はこのコマンドでは行わない。読み取り済みのデータだけを見るので送信も課金も発生せず、中間データも書き換えない。出力は1件ずつの行（どの紙のどの欄か・主枠の字数・期待する桁数・右隣の字数）と合計1行で、書かれていた内容と座標は出さない。期待する桁数は欄の名前から導いており、その根拠も出力に載せる（テンプレートの決まりに桁数を書く場所が無いため）。GUI からは呼べない（画面用の許可リストに入れていない）
- **API 送信の月次上限による強制停止（`config.json` の `api_monthly_cap`・既定 900 ユニット）**: 上限に達したら警告ではなく送信そのものを止める（課金は後から取り消せないため）。残量は `verify` と GUI の実行画面に出る。カウンタは `%LOCALAPPDATA%\ChouhyoOCR\api_usage.json` に置き `purge` では消さない。0.1.0 の後（2026-08-28）に入った変更だが本書に記載が無かったため、ここで補う (#53 L-2)
- **性能計測に枠検出と累積レンダーの2モード（`scripts/perf_check.py`・#87・#100）**: `--only frames` は「ページ全体から枠候補を生成」1 枚の所要時間を sample-1・formB・formC の3素材で測り、予算 3.0 秒（NFR-F02）と比べる。線が多すぎて打ち切る案内が出る条件（軸ごとのレール数 200 超）も合成画像で毎回確かめる。`--only cumulative --runs N --pages M` は同じ作業フォルダへ M ページずつ足しながら N 回読み取り、1 回ごとの出力生成時間と累積ページ数を並べる——出力は毎回すべてのページを作り直すので、件数に比例して伸びる形を数字で見るためのもの。既定（`--only all`）の動きは従来どおりで、2つとも opt-in
- **配布物のサードパーティ・ライセンス表記（`THIRD-PARTY-NOTICES.txt`・#78）**: 配布物に含まれる他社ソフトウェアの著作権表示と許諾条文を 1 ファイルにまとめ、NSIS インストーラでアプリと同じ場所へ入れる（`tauri.conf.json` の `bundle.resources`）。中身は手書きせず `scripts/gen_notices.py` で生成する——Rust 側は `cargo about`（`gui/src-tauri/about.toml`・Windows 64bit 向けに絞って 216 クレート）、Python 側は `pip-licenses`（PyInstaller が実行ファイルへ取り込んだ 36 パッケージ）。Python 側の対象は「開発環境に入っているもの」ではなくビルドの中間生成物（`workdir_build/pyi/chouhyo-core/*.toc`）から割り出すので、pytest・playwright のような開発専用の依存は自動的に外れる。`gen_notices.py --check` で依存と表記の食い違いを検出できる。あわせて `cargo deny`（`gui/src-tauri/deny.toml`）でライセンス許可リストと RustSec の脆弱性 DB を点検する（2026-09-03 実行時点で脆弱性 0 件・コピーレフト 0 件）
- **GitHub 側での混入検知（`.github/workflows/guard.yml`・#98）**: 全ブランチへの push と Pull Request のたびに、その差分のパスと中身を検査して該当があれば失敗させる。手元の `.githooks/pre-commit` は clone ごとの手動配線で `--no-verify` でも外せるため、サーバー側にもう一枚置いた。**これは検知であって防止ではない**——ワークフローが走る時点で blob は GitHub に到達済みで、公開リポジトリなのでその瞬間から誰でも読める。止める役はフック・secret scanning の push protection（2026-09-03 時点で有効）・branch protection の required check が担う。起点は main への push が `before`、それ以外のブランチは `origin/main` との分岐点（新規ブランチの初回 push で全量走査へ落ちるのを避けるため）。push では前の run を kill しない（連続 push で先行の差分が無検査になるため）。検出しても中身は出さず、ファイル名と種別だけをログに出す（公開リポジトリでは実行ログを誰でも読めるため）。検出パターンはワークフロー自身に完全な形で書かず断片から組み立てる——そのまま書くと guard.yml を検査対象から外す必要が生じ、そこが穴になる
- **回帰ゲートの CI 実行（`.github/workflows/ci.yml`・#95）**: Pull Request は ubuntu-latest で軽量（型検査・GUI 純ロジック・Python 依存の解決）、main への push は windows-latest でフル（pytest 全体・GUI 純ロジック・cargo test）。ランナーは標準のみ。CI では `run_all_tests.py` ではなく pytest を直接呼ぶ——テストの約 38% が `.gitignore` 済みの `workdir/` 素材に依存して skip するため、ランナーの skip 率ガード（10%）が CI では必ず FAIL になる。CI の判定は「skip は許すが failed は 0」。core は Windows 専用（`cred_store.py` の `ctypes.wintypes`・`msvcrt` のファイルロック・`.venv/Scripts/python.exe` を起動する 6 テストファイル）のため、pytest を ubuntu へ割ることはしない
- **カバレッジ計測の開始（`run_all_tests.py --coverage`・#65-5）**: 既定 OFF のフラグを足し、付けたときだけ pytest に `--cov=chouhyo_ocr` を渡してサマリ行へ `coverage: NN.N%` を出す。普段のゲートの所要時間は変えない。初回計測は **83.0%**（2026-09-03・3,999 文中 3,321 文。Wave 1 の編集中に測った暫定値）。行ごとの内訳は `workdir_build/coverage/coverage.json`（gitignore 済み）。CI には載せていない——まず手元で運用する。**合格ラインは未定義**（リポジトリのどの文書にも閾値の記述が無いことを走査で確認済み）
- **回帰ゲートに TypeScript の型検査を追加（#95）**: `scripts/run_all_tests.py` が `tsc --noEmit` を実行する。GUI の純ロジックテストは esbuild で型注釈を剥がすだけで型を見ず、`tsc` が走るのは配布物ビルドの時だけだったため、型エラーの露見が遅れていた
- **Python 依存の宣言（`core/pyproject.toml`・#94）**: `[build-system]`・`[project.dependencies]`（google-cloud-vision・jsonschema・numpy・openpyxl・pillow）・`[project.optional-dependencies].dev`（pytest・pytest-xdist・playwright・pyinstaller・psutil）をバージョン範囲付きで宣言し、`pip install -e core[dev]` に一本化した。README の個別列挙には `pytest-xdist` が無く、`run_all_tests.py` が無条件で渡す `-n auto` のせいで、README どおりに作った環境では一括テストが起動できなかった
- **ブロック単位の枠吸着（枠判定の自動化・段 (f)・#75・既定 OFF）**: 表のブロックごとに、テンプレートの枠を紙の罫線へ縦方向だけ合わせ直す。紙の伸縮で「左のブロックは合うのに右のブロックだけ数 px ずれる」入力を、面全体のずれ補正（従来から入っている）で取り切れなかったぶんだけ動かして拾う。**既定は無効で、`config.json` に `snap_blocks: true` を書いたときだけ動く**——枠を動かした結果が正しいかを機械で確かめる手段が無いため、実データで確かめるまで既定は変えない（設定画面には出さない）。動かす量は、期待する罫線の位置と実際に検出した罫線との**ずれの中央値**（面全体のずれ補正を当てた後に残った分）をそのまま使う。動かす量の上限は行と行の隙間（テンプレートから計算する。出荷テンプレートで front 8px・back 4px）で、上限を超える・罫線の対応が取れない（一致本数が期待の半分未満）・**動かすと同じ面の別の欄と新しく重なる**のいずれかに当たったら、その面は1ブロックも動かさずテンプレートの位置で読む。3つ目は出荷テンプレートの実測から入れた条件で、front の表の最終行の下端（y=1464）と「備考」欄の上端（y=1471）の余白は 7px しかなく、上限の 8px を使い切ると 1px 重なる——重なると読み取った文字がどちらの欄に入るかが定義順で決まり、正常の顔のまま値が入れ替わる。横方向は動かさない（列と列が接していて安全に動かせる量が 0 のため）。行が 3 行以下の表は合わせ先として信用せず、その表を含む面ごと対象外にする（テンプレートを読み込んだ時点で警告 W-7 が出る）。動かさなかった件数（入力の紙由来）と対象外だった件数（テンプレート由来）は原因が違うので、実行結果に別々の数字で出す。動かした量・一致本数・理由は中間データに残り、`run` の後の `remap`・`debug-images`・中断からの再開はすべて同じ座標を復元する。**吸着の ON/OFF は中間データに記録して再利用時に照合する**——ON で読み取ったデータを OFF の設定で使い回そうとすると拒否される。位置合わせ方式の版（`algo_version`）は上げていないので、**これまでどおり OFF で使う限り、既存の作業フォルダはそのまま使えて再送信（課金）は発生しない**。単発の欄・除外領域・面の切り出し範囲は動かさない。実行結果の 2 つの件数は「入力した紙の状態で決まるので読み取るたびに変わる」「テンプレート側の性質なので毎回同じ」と区別して案内する。ON にする前の確認用に、読み取り済みのデータだけで ON と OFF の差を数える点検コマンド `snap-diff` を足した——**数えるだけで、送信も書き換えもしない**（中間データは一時的な複製から読む）。出すのは件数と「どの紙のどの列か」までで、書かれていた内容は出さない。合わせた結果が正しいかどうかは機械では確かめられないので、差の出た欄を人が見て判断する。OFF のまま読み取ったデータには比べる相手が無いため、理由を出して何もしない。要件 07 v1.5・設計 08 §6
- **位置合わせ残差の記録（枠判定の自動化・段 (c)・#74）**: 読み取り結果を変えずに、位置合わせで枠がどれだけズレて着地したかを残す。面ごとに「期待線＋シフト量」と最も近い検出線との符号付き距離を軸別（中央値・最大・対応本数・対応なし本数）とブロック別（y のみ）に集計し、中間データの `alignment` に `align_residual_px`（面の代表値・-1＝未計測）と `align_residual_detail`（JSON）として記録、ログに `align_residual` イベント（面ごと 1 件・`face_idx` のみで名前は出さない）を出す。`expand-page` の応答に `faces[].residual` をキー追加のみで載せる（位置合わせに失敗した面と再利用ページは未計測のままキーを出さない）。対応窓は既存の `shift_limits` を流用し新しい px 定数は作らない。判定（`ok`・`reason`・`dx`・`dy`・`matched`・`total`）の算出経路と画像走査は変えず、`ALGO_VERSION` も上げない——golden 出力と一致（`testdata/golden_manifest.json`・2026-09-03 実測）。ブロック別に持つのは、面の合併集合から求めるとブロック間のズレが打ち消し合って消えるため（片ブロックだけ 4px 動かした合成画像で実証）。画面表示は入れていない（Q-F5b 未回答・core は常に値を返し編集画面は受け取るだけ）。設計 08 §5
- **ページ全体からの枠候補一括生成（枠判定の自動化・段 (b)・#73）**: テンプレート編集画面の「ページ全体から枠候補を生成」で、開いている帳票の罫線から表候補・欄候補をまとめて作る（領域を選ぶ必要がない）。候補は破線＋隅マーカー＋`?` 付きの連番で確定枠と区別し、一覧でチェックしたぶんだけを一括採用する（個別の採用・除去も可）。**採用しても既存の枠は削らない**——重なる候補は一括採用の対象から外し、個別に採用するときだけ確認を挟む（従来の枠追加は下にある欄を自動で切り抜くが、候補の採用はこの経路を通らない）。表候補は採用後に「接頭辞」で列名（`列1`…）を一括で置き換えられる。候補が0件のときは理由を日本語で出す（罫線が無い／閉じた枠になっていない／用紙の外枠だけ／線が多すぎて打ち切り）。検出はしたが候補にしなかった枠も内訳を件数で示す（用紙の外枠・小さすぎる・面をまたぐ・四角形になっていない）。生成は Ctrl+Z で1コマ戻る。CLI は `detect-frames --input <画像|PDF> [--page N] [--dpi N] [--template <path>]`（PDF の展開先は `workdir/detect_frames_pages/`。`--template` を渡しても入力画像の寸法がテンプレートと違うときは面の割り当てと重なり判定を行わず、その旨を返す）。既存の「くり返し行」からの生成（`detect-grid`・選択範囲が必要）はそのまま残る。formB（1800×1200）で表候補1＋欄候補3を 0.23 秒、formC（2490×3510・37×9 レール）を 1.01 秒で処理（2026-09-03 実測・`detect_frames` 単体）。要件 07 v1.3・設計 08 §4
- **テンプレートの保存・選択・照合提示（枠判定の自動化・段 (t)・#72）**: 利用者テンプレートを `%APPDATA%\com.holodev.chouhyo-ocr\templates_user\` に名前を付けて保存し（許可リスト方式の名前検証・NFC 正規化は既存依存の icu_normalizer・保存は staged→verify→promote）、実行タブ上部の選択で読み取りに使える。画像を開くと出荷テンプレートと利用者テンプレートに様式判定を掛けて「この画像に合うテンプレート」を提示（スコアが近いときは推奨なし・名前順）。前回使ったテンプレートは `config.json` の `last_template`（`shipped` または `user:<名前>`・絶対パスは保存しない）で復元。書き出し／取り込み、様式が合わない紙から「この紙用に新しいテンプレートを作る」（空テンプレート）。列挙とパス検査は Rust に一本化し、core は渡されたパスだけを読む（`match-templates`）。要件 07 v1.3・設計 08 §3
- **様式判定の接続（枠判定の自動化・段 (a')・#71）**: 既存の位置合わせ判定を 一致／不一致／判定不能 の3値に再分類し、テンプレート編集で PDF・画像を開いたとき様式が合わない面の枠を描かない（「それでもこのテンプレートで開く」で表示）。判定不能（罫線が乏しい・上端がかすれた本物の紙）は従来どおり枠を描く。読み取りでは無関係な紙が送信前に `様式不一致` として止まり、実行画面に理由コードと「送信前・課金なし」の件数、出口2択（別テンプレートを選ぶ／テンプレート編集で作る）を出す。全ページのスコアと理由コードを中間データとログに記録し、判定処理の例外は `format_check_failed` として `様式不一致` に化けない。新しい画像処理は足しておらず、読み取り出力は着手前と一致（golden 比較）。要件 07 v1.2・設計 08
- **表（くり返し行）を持たない紙をテンプレートとして使えるようにした（#86）**: 申込書のように単発の欄だけが並ぶ紙は、これまで「位置合わせの目印が無い」としてテンプレートの読み込み自体を拒否していた。欄の枠線（矩形の4辺）を目印に使えるようにし、拒否は「横向き・縦向きのどちらかの目印が1本も作れない面」だけに狭めた。目印に採るのは幅・高さが 40px（300dpi 換算・※要較正）以上ある辺で、短い辺は文字の一画を線と取り違えるため使わない。目印になる欄が3個未満の面は読み込めるが警告（W-5）を出す。検出は面の全体ではなく期待位置の周辺だけを見る——全体を見ると印字文が目印に混ざり、正しい紙を「様式不一致」として送信前に止めてしまうため。ずれの探索範囲は表とは別に固定 24px（※要較正）。**表を持つ面はこれまでと別の経路を通るので、既存テンプレートの読み取り結果・位置合わせの残差・様式判定は 1 ビットも変わらない**（golden 出力一致・`testdata/golden_manifest.json`・2026-09-03 実測）。位置合わせ方式の版（`algo_version`）も据え置きなので、読み取り済みデータの再送信は発生しない。GUI の変更は無い。設計 02 §6.2 手順6・D-25
  - **ずれたまま読み取ってしまう経路を塞いだ**: 同じ大きさの欄が等間隔に並ぶ紙（縦積み・横並びのどちらも）で、紙が1段ぶんずれてスキャンされても、以前はずれに気づかずそのまま読み取っていた。面の中でいちばん外側にある目印の線（上端・下端・左端・右端の4本）が紙の線と合うことを確かめる検査を足し、合わなければ「位置合わせ失敗」として送信前に止める。ずれが残っていれば必ずこの4本のどれかが外れるので、1段ぶんに限らずどんなずれ量でも同じ検査で捉えられる。**この検査は面のいちばん外側の欄に枠線が印刷されていることを前提にする**——たとえば最上段の欄が下線だけで上の枠線が無い紙では、正しい紙でも毎回「位置合わせ失敗」になる（黙って間違った値を出すよりは安全側だが、そのテンプレートは使えない）。目印の少ない面に出る警告（W-5）でこの点を案内する
- **汎用化第1弾（様式Bの実測）**: v1 と構成の異なる第2様式（単一面・表裏分割なし・24列）がコード無改変・テンプレート差し替えだけで `run` 完走・配置取り違え0件になることを実測。様式依存の焼き付き（face_id 2値固定・表必須・1帳票=1ページ等）と新発見4件を docs/design/chouhyo-ocr/06_second_form_findings.md に一覧化した。検証資産は `testdata/formB/`（配布物に混ぜず・git 追跡で再現可能）
- **px 定数の dpi 正規化**: 300dpi 較正だった内部定数（行クラスタ間隔・空間インデックス・罫線検出・位置合わせ・選択肢マーク検証の許容値）をテンプレートの `render_dpi` でスケールする。300dpi では従来と完全一致（バイト一致・全421テスト緑）。`detect-grid` に `--dpi`（72〜1200・範囲外は拒否）を追加。元号丸印の帯定数は実測較正済みのため意図的に対象外（残件は 06 §7）
- **`verify --expect-columns N`**: 導出列数が N 列未満なら template チェックを NG にする最小列数ゲート。取り込み先と列構成を合わせた後の突き合わせに使う（N 以上は OK・省略時は挙動不変）(#65)
- **実行画面のカウンタ通知**: 参照先の文字の採用／破棄・切り抜きの穴に落ちた文字（その欄は〓になる）・主と参照先の食い違いを、出力しない欄由来の内訳付きで run / remap 完了時の「実行時のお知らせ」に表示。CLI サマリにしか出ていなかった課金・〓の動く分岐が GUI でも見える (#65)
- **欄単位の出力対象外**: テンプレート編集画面の「出力する」チェックで、枠・読み取り・中間データを保ったまま列だけを外せる。JSON では `output: false` のときだけ書く（省略＝出力）。解除は `remap` のみで復元され API 再送信は発生しない。全欄を外すテンプレートは保存拒否 (#66)
- **列の並べ替え**: 出力列タブの上下ボタン（面内の欄どうし）と表パネルの列入替（表の内部列——220列中200列を動かす本体）。管理6列は先頭固定。並べ替えを含む保存は枠の自動調整と両立しないため座標が動く場合は保存を中止する (#66)
- **列位置表示**: 選択中の欄が出力の何列目かを表示（verify の `column_names` を唯一の正とし GUI 側では再導出しない）(#66)
- **列名一覧ファイル**: 出力時に `output_<日時>_columns.txt` を併記（取り込み先システムとの列構成突き合わせ用・xlsx/csv と同じアトミック差し替えの対象）(#66)
- **保存前確認の統合モーダル**: 出荷テンプレ上書き・画像寸法差・除外領域の変化・出力列の減少を1枚に統合（alertdialog の ARIA・Tab トラップ付き・警告ゼロなら出さない）(#66)
- **テンプレート検証の追加**: verify に `column_names`・`output_disabled_cells`、W-4（切り抜き穴どうしの重なり＝配列順依存の警告）(#66)
- **文字単位〓**: `config.json` の `unclear_char_level` を有効にすると、低信頼の文字だけを〓へ置き換える（「旭川市」→「旭〓市」）。対象は text 欄のみ、既定は OFF の段階導入 (#62)
- **参照先から採った値の由来印**: 中間データの `cell.origin` と xlsx の淡青セル色で、主の欄ではなく参照先から拾った値だと分かる。要確認セル数には数えない (#54)
- **テンプレート検証の警告と可視化**: 除外領域と受け皿の重なり（W-1/W-2）、受け皿どうしの隣接死角（W-3）を検証時に報告し、`verify` は除外領域の件数を面ごとに返す。出荷テンプレートに意図的な重なりが実在するため拒否はせず、GUI は黄帯で示す (#55, #59 H-6, #61 L-4)
- **保存時の前後差分表示**: 欄・金額列・除外領域の増減を読み込み時点と比べて出し、減っている場合は強調して確認を求める (#55, #59 H-9)
- **`expand-page` の `--no-mask` と失敗理由**: 編集下地から白塗りを外して枠を合わせられるようにし、失敗は握り潰さず `reason`（template / align / image / other）で返す。`run` が送信する画像のマスクは従来どおり (#59 H-8, #60 追補)
- **`debug-images` の安全策**: 中間データと異なるテンプレートでの可視化を拒否するゲートと、生成0件時の理由（`no_pages` / `page_not_found` / `no_aligned_images`）(#60 M-1)
- **`run` / `remap` サマリのカウンタ**: `fallback_used` / `fallback_discarded` / `carve_hole` (#54, #59 H-4)
- **`run` ログのテンプレート出所**: `run_start` の `template_path` と `template_loaded` の `template_hash`。記入値は含めない (#58)

- **表の選択式列に選択肢を入力できるようにした（#53 L-6）**: テンプレート編集で表の列の種類を「選択式」へ変えても、丸印を探す範囲（選択肢）を作る手段が画面に無く、保存すると必ず検証で拒否されていた——テンプレートの決まりで、選択式の列には違う選択肢が2つ以上必要になる。列の行に「選択肢」欄を出し、`昭,平,令` のようにカンマ区切りで入れると、列の幅を等分した判定範囲を作る。足りない間はその行に理由を出し、保存も「選択肢が足りません」と直し方を添えて止める（以前は決まりに合わないという表示だけで、どこをどう直すか分からなかった）。行の中で上下に積む様式の微調整は、従来どおり保存後の JSON で行う

### Changed

- **〓の基準値を既定より下げたときに注意を出すようにした（#69 L-S5）**: 設定の「〓と判定する基準値」は 0.01 まで下げられる。0 に近い値は〓がほとんど出ない設定で、目視確認の前提（要確認セルを全部見る）が崩れる。止めはしない（正当な調整もありうる）が、既定（0.85）より低い値のときは「〓が減るぶん、読み誤りの見落としが増えます」と入力欄の下に出す
- **送信する画像の圧縮率を明示的に固定した（#52 M-15）**: これまで Pillow の既定値任せだった PNG の圧縮率を、同じ値（6）で明示指定に変えた。**出来上がるファイルは1バイトも変わらない**（未指定と 6 が同一であることを実測で確認済み）。固定するのは、将来ライブラリ側の既定値が変わると、こちらは何も変えていないのに送信する画像のバイト列が変わり、保存済みの応答が「別の画像に対するもの」と判定されて**全ページ再送＝再課金**になるため。圧縮率を下げれば1枚あたり 0.17〜0.36 秒速くなるが、そのぶんファイルが 4〜46% 大きくなって送信時間が延びる（上りが 5Mbps を下回る回線では差し引きで遅くなる計算）ことと、上記の再送が現に起きることから、速さは取らなかった
- **テンプレート読み込みのたびにスキーマ自体を検証し直していたのをやめた（#86 同レーン）**: テンプレートの検証には JSON Schema を使っているが、これまでは 1 件読むたびに「スキーマ自体が正しいか」の点検（`check_schema`）からやり直していた。検証器を作るときに 1 回だけ点検し、以降は使い回すようにした。候補7件ぶんの読み込みは1377 → 620 ms（2026-09-03 実測・3 試行の最小値）。エラーの選び方・メッセージの文面は変えていない（`jsonschema.validate` と同じ手順を踏む）
- **テンプレート照合が候補ごとにやり直していた前処理をやめた（#82）**: 画像を開いたときの「この画像に合うテンプレート」は、候補1件ごとに画像の縮尺合わせ・面の切り出し・二値化・傾き推定をやり直していた。この前処理は候補の中身（期待する罫線の位置）に依存しないので、面の形が同じ候補どうしで使い回すようにした。候補7件の照合時間は 4077 → 1021 ms（4.0 倍・sample-1 2490×3510・2026-09-03 実測）。時間切れで候補を打ち切る動きが減り、同じ条件の実走で評価できた候補が 1〜5 件から 6〜7 件になった。判定（一致／不一致／判定不能・スコア・理由）は分離前と一致することを素材3×テンプレート2の全組み合わせで固定してある。打ち切りの予算（合計 3.0 秒）と候補の件数上限（20）は変えていない
- **道具が無い環境を SKIP ではなく FAIL にした（#95）**: `run_all_tests.py` は node / cargo が無いと GUI・Rust のテストを SKIP 扱いにし、集計の合否に効かせていなかった。テストが1件も走っていないのに PASS が出る状態だったため FAIL に変え、導入方法をメッセージに添えた
- **セットアップ手順を宣言からの導入に一本化（#94）**: README の `pip install` はパッケージ名を並べる形をやめ、`pip install -e "core[dev]"` にした。パッケージ名の二重管理をやめる。あわせて実際には import していない keyring を外した
- **枠候補の残差と除外内訳の精度（#85）**: 表候補の「残差」が2行の候補でつねに 0 になり信頼度の判断に使えなかったのを、行の等間隔からのずれと罫線位置のばらつきの大きい方を採る形にした（sample-1 で 2行候補8件すべて 0.0 → 0.7〜2.1・2026-09-03 実測）。四方が線でつながっていない枠は検出内訳のどこにも出ていなかったので `not_closed` として件数に加え、「検出した連結成分＝候補にした枠＋除外した枠」が数として合うようにした（sample-1 で 11 件・JSON はキー追加のみで既存キーの意味は変えない）
- **テンプレート編集の枠生成を2つの入口に分けた**: 従来の「くり返し行（家族・明細）」で外枠を描いてから罫線検出する経路は挙動を変えずそのままで、領域を選ばずページ全体を解析する入口を隣に足した。生成中はキャンバスの枠操作とツールボタンを無効化する（パンとズームは可）。候補パネルは確定枠の一覧とは別に持ち、保存対象には入らない——テンプレートを読み込み直したり別の画像を開いたりすると候補は消える（前の画像の座標で描いた枠を残さないため）
- **完了サマリに再利用ページ数の注記**: 同じ workdir で処理済みのページは Vision へ再送せず再利用するため、入力枚数と API 送信回数が合わないことが完了画面から読めなかった。`summary.reused_pages` を追加し「既存の読み取り結果を再利用: N ページ（送信なし）」を表示（実機の通し確認で発見）
- **様式判定を無視した後の導線**: 常時警告に別のテンプレートを試す方法を添え、「判定に戻す」で枠を隠した状態へ戻せる
- **テンプレート編集の初期表示**: 出荷テンプレートの自動読み込みは残したまま、帳票の画像か PDF を開くまでキャンバスに枠を描かないようにした。画像が無い間は用紙サイズの白い下地と、画面中央の案内「テンプレート（id）を読み込み済み・欄 N・表 M。帳票の画像か PDF を開くと、枠が記入欄に重ねて表示されます」を出す（テンプレートを読めていない場合はその旨に切り替わる）。枠を扱うツールボタン・側パネルの追加系ボタンは無効化し、キャンバスのドラッグと矢印キー移動も受け付けない（パンとズームは可）。案内はメッセージ欄にも `aria-live` 付きで出す。8/31 の「白紙で始まると誤解される」対応で、画像より先に枠だけが暗い背景に浮く見た目になっていた点への手当て（2026-09-02 ユーザー指摘）
- **`render_dpi` が 300 以外で、選択式（choice）欄を持つテンプレートを読み込み拒否**: px 定数の dpi 正規化から元号丸印の帯定数だけを外してある（実測較正のため機械スケールできない）。300dpi 以外で丸印判定を走らせると静かに外れるため、再較正まで `load_template` で止める。レビューでの初案は警告だったが、警告では気づかれずに出力まで通る (#69 Q-MA)
- **`run` / `remap` サマリに `total_done_pages`（中間データに溜まった `state='done'` の累積件数）と `render_seconds`（末尾の書き出しの実測秒）を追加**: GUI は累積 1,000 頁を超えると完了画面で purge を促す。`remap` は render を呼ばないため `render_seconds` を付けない (#69 P-H1)
- **回帰テストを並列化**: `run_all_tests.py` の pytest に pytest-xdist（`-n auto`）を導入し、フルゲートを 490.7s → 198.4s へ短縮（同一件数・全緑を実測確認。テストは tmp_path 隔離・共有素材は読み取りのみのため並列安全）
- **実行画面の起動時説明文を削除し、ウィンドウを状態連動サイズへ**: 「このツールの動作」ブロックと補足文を除去し、右カラムを廃止して単一カラム化（旧説明枠の空白を残さない）。既定サイズは 1280×860 → 730×620（実行前の全状態で余白・スクロールが出ない実測値）。処理完了でサマリが出ると必要な高さまで自動で縦に拡大し（上限はモニタの作業領域・上限超過時のみスクロール）、テンプレート編集タブでは 1280×860、実行タブへ戻ると状態に応じたサイズへ戻る。ブラウザのデモモードではリサイズしない
- **実行画面の「一時停止」を Won't 化（要件 v3.15）**: 中断→再実行が未処理分のみ再送（追加課金なし）で用途を代替できるため、GUI→コアの双方向制御チャネルは新設しない。要件 §5.10・設計 §7.1 を改訂
- **参照先の空判定を3分岐化**: 主と参照先それぞれの文字数の組み合わせで判定する。ノイズ1文字で参照先が永久に無効化される状態を解消した (#54)
- **複数領域の文字連結順を「領域→帯→行→x」へ**: コの字の欄で文字が交互に混ざり、L字で行が融合していた。定義順での連結は住所で逆順になるため採らなかった (#57)
- **テンプレート保存をトランザクション化**: 一時ファイルへ書いてコア検証が通ったときだけ差し替える（`.bak` 退避つき）。検証が通らなければ元ファイルは無傷で「保存していません」と明示する。保存先の既定も読み込んだファイルへ変え、出荷テンプレートの上書きは明示確認とした (#56)
- **〓の集計3経路（xlsx の COUNTIF・条件付き書式・csv）を `unclear_char_level` でゲート**: OFF（既定）は従来の完全一致のまま——COUNTIF 数式まで機能追加前と一致することを実データで確認済み。ON のときだけ「〓を含む」判定＋一部〓の第2書式へ切り替わる（完全一致のままでは文字単位〓が要確認セル数に乗らず「合計0で完成」の出荷ゲートが嘘をつくため）。Excel 実機での `"*〓*"` の評価は 2026-09-01 に Excel COM で確認済み——一部〓「旭〓市」が1として数えられ csv と一致（証跡は 04_unclear_policy.md §8.3）。既定 ON への反転は従来どおり実データ1バッチ確認後 (#62)
- **エディタの自動切り抜きを3段階判定へ**: 面積の減少率で自動適用・確認・人へ返すを分け、表裏の境界をまたぐ切り抜きは自動適用しない（CSV の列ずれを防ぐ）(#59 H-2, H-4)
- **GUI からのコア起動でテンプレートを明示指定**: エディタの編集対象と `run` の参照先が別実体になり、保存しても実行に反映されなかった (#58)
- **配布物へ複製するテンプレートを出荷テンプレート1件に限定**: 下書きが混入すると、実行時にどれが使われるか分からなくなる (#59 H-7)
- **エディタの再描画を requestAnimationFrame で間引き**: `mousemove` のたびに全欄の `measureText` が走っていた (#60 M-3)
- **設計正本（要件・設計・ロードマップ・テスト要件）を現状へ追従**: 列数はテンプレートからの導出結果（現行220列）とし、D-24（郵便番号を住所へ統合）は差し戻し済みと明記した (#60 M-5)

- **確認のダイアログを画面の中へ移した（#87）**: 重なっている枠候補を個別に採用するときの確認と、未保存の変更を捨てて別のファイルを開くときの確認は、OS のダイアログだった。長い説明が折り返されず読みにくいうえ、ボタンが「OK / キャンセル」固定で押した先に何が起きるか読めない。保存前確認と同じ作りの画面内ダイアログへ変え、ボタンを「採用する／中止」「破棄して続ける／戻る」にした。Esc で中止でき、開いた直後のフォーカスは何も変えない側にある（そのまま Enter を押しても先へは進まない）。破棄の側は目立つ色のボタンにしない——押すのが既定の操作に見えてしまうため。本文も「元に戻せません」を問いより先に置いた
- **選択中の枠の色を濃くした（#67）**: 選択中であることを示す黄色は、白い紙面に対して 1.41:1 しかなく、枠が紙に溶けていた（アクセシビリティの基準は 3:1）。同じ黄系のまま明度を下げた色に置き換え、紙面で 4.69:1・画像の外側の暗い下地で 3.51:1 になった。色の違いだけに頼らないよう、選択中の枠は線も 1px 太くしている。キーボードで操作したときに今どこにいるかを示す枠も、ブラウザ任せをやめて色と太さを決めた。出力しない欄に重ねる斜線も 30% から 45% へ濃くしている
- **枠候補の一覧を1行2段にして、一度に見える件数を増やした（#87）**: 種別と番号・寸法と残差・採用・除去を横1列に詰めていたため、幅 320px のパネルでは文字が折り返して行の高さがまちまちになり、候補が多い紙では一覧のほとんどが画面の外にあった。情報を2段に分けてボタンを両段にまたがらせ、行の高さを一定にした。2段目の書式は他のパネルの補足文と同じ小ささに揃えた——揃っていなかったため、狭い幅では寸法と残差が真っ先に省略されていた（省略される割合は実測で 25.5% → 5.3%）。「採用」は輪郭を強めたボタンにして「除去」と見分けられるようにした
- **保存に成功したときの表示を2段までにした（#65-7）**: 保存は成功しているのに「切り抜けない欄があります」の赤帯が残り、灰色の成功メッセージ・赤帯・黄色い注意の3段が同時に出ることがあった。赤帯は「保存していません」の意味に限り、成功時の注意（読み込み時より減った項目・切り抜けなかった欄・大きく切り抜かれた欄・コアからの警告）は黄色い注意1段へまとめた。あわせて成功そのものを緑の帯で受けるようにし、読み込み時からの差分は帯の2行目へ回した——灰色の1行はツールバーの右端に流れ、長い差分の末尾が見切れていた。この注意帯は読み上げの対象にもした
- **検証素材の置き場を中間データから分けた（#88）**: 実サンプル画像・保存済み Vision 応答・golden 出力を `workdir/` から `testdata/local/` へ移した。以前は中間データと同じフォルダに同居していたため、`purge --yes` を一度実行するだけで golden 比較（AC-F45）と `perf_check.py` の性能計測が再現できなくなっていた——検証の土台を自分で消す構造だった。移設先も記入値を含むので Git の管理対象には入れない（`.gitignore` に `/testdata/local/`）。`purge` 側は特別扱いを増やしていない（残すものは資格情報だけ・個人情報に対して fail-closed という #83 の方針のまま）。参照する 47 ファイル（テスト 32・スクリプト 3・設計文書 5・その他 7）を新しいパスへ追従させ、golden の出力が移設前と一致することを確認した（`cell_value_sha256` `4030f5e3…`・`normalized_sha256` `1ec2ff9c…`・2026-09-03 実測）

### Fixed

- **様式を判定できなかった紙が、様式の合った紙とまったく同じに見えていた（#71 AC-F11）**: テンプレート編集で画像を開いたとき、様式を判定できなかった（罫線が読み取れない等）場合も枠は一致時と同じ色・同じ実線で描かれ、案内は画面右上の小さな灰色文字に出るだけだった。合っていない可能性のある枠を「合っている」と読み違える作りだったため、この場合の枠を薄い破線（参照先の破線とは別の間隔）にし、案内を黄色の帯へ移して先頭に「※判定できませんでした:」を付けた。枠を消さないこと・「枠は動かさないでください」という指示はこれまでどおり
- **出力に失敗した行が「様式不一致」と表示されていた（#80）**: 送信済みのデータから Excel の行を組み立てる段で失敗したページに、「様式不一致」が付いていた。テンプレートは合っているのに合っていないと表示されるため、利用者は原因のない場所を探すことになる。**.xlsx の「ステータス」列に `出力失敗` が現れるようになった**（ステータス列を機械で集計している場合は、この値の追加に対応が要る）。実行画面には原因の別も出る——中間データが壊れている可能性か、プログラムの不具合の可能性か。あわせて、失敗したページを次回の読み取りで送り直さないようにしてある（送信済み＝課金済みのため）。原因が解消して出力し直すと、この表示は自動で消える
- **全ページが様式不一致で終わっても、緑の完了バナーが「成功」に見えていた（#69 残置1）**: 1件も送信せず全ページが様式不一致だった実行でも、画面のいちばん上に緑の「読み取りが完了しました」が出ていた。この条件のときはバナーを注意色（黄）にする。文言は変えていない——何が起きたかは既存の赤帯（テンプレートを選び直すか作る、という案内）が説明している
- **同期フォルダと判定されると、逃げ道なく使えなくなっていた（#52 M-12・#69 Q-MJ）**: 保存先がクラウド同期の下だと読み取りを開始できないが、この判定は名前で広めに拾うため誤検知しうる。誤検知に当たると設定を変える以外に手が無かった。警告カードに「同期される場所であることを理解したうえで読み取りを開始する」のチェックを1つ足し、入れた回だけ開始できるようにした。チェックは保存しない（毎回確認する）
- **設定の保存が、触っていない項目まで書き込んでいた（#69 Q-MF）**: 設定画面は6項目をまとめて送っていたため、`config.json` に書かれていない項目（＝コア側の既定に従っていた項目）が、1項目を保存しただけで画面上の既定値として固定されていた。実際に変わったキーだけを送るようにした。何も変えずに保存したときは書き込み自体を行わない
- **失敗したページの一覧が上限なく伸びていた（#53 L-17）**: 詳細ログは 400 行で打ち切っていたのに、失敗一覧は件数の上限が無く、数千件の失敗で描画が重くなりうる状態だった。一覧は先頭 400 件までにし、件数は全件を数えて「他 N 件」と添える。末尾ではなく先頭を残すのは、原因の切り分けでは最初に出た失敗の並びが手掛かりになるため
- **中断ボタンが、再利用された別プロセスを止めうる隙間があった（#53 L-13）**: 読み取りの終了直後、プロセスの管理番号（PID）を画面側の記録から消すまでの間に「中断」を押すと、その番号が別のプロセスに再利用されていた場合にそちらを止めうる。プロセスの手掛かりを握ったまま記録を消す順序に変え、隙間そのものを無くした
- **テンプレート保存のバックアップ（`.bak`）に掃除する契機が無かった（#65-11）**: 保存のたびに直前の内容を `<保存先>.bak` へ残すが、消える契機が無く、消したテンプレートの分も含めてフォルダに残り続けていた。保存に成功したときに、同じフォルダの `.json.bak` のうち更新から7日を過ぎたものを掃除する（今回作った1世代は残す）。世代方式ではなく日付方式にしたのは、`.bak` の名前が保存先ごとに固定で、同名の世代がそもそも1つしか存在しないため——実際に溜まるのは別のテンプレートの分と、本体を消した後に残る孤児で、これらは更新日時でしか区別できない
- **元号の丸を測る範囲が隣の欄へはみ出していた（#52 M-1）**: 昭・平・令のどれを丸で囲んだかは、印字文字の左右に細い帯を取ってインクの量を比べて決める。この帯が用紙の端までしか制限されておらず、出荷テンプレートの家族欄では帯 14px のうち 9px が隣の手書き日付欄の内側に入っていた——隣の欄の左端に数字が書かれていると、丸の有無ではなく隣に何が書かれているかで順位が動きうる。帯を欄の枠（＋はみ出しの許容 4px）で切るようにした。許容を 0 にしないのは、手書きの丸が枠を数 px 越えて描かれるためで、0 にすると実サンプルの1件が「丸なし」に反転する（実測: トップ値 0.0676 → 0.0477）。手持ち2枚8箇所の正解率は 8/8 のまま、判定の余裕はむしろ広がった（1位2位差の最小 0.0647 → 0.0669・2026-09-03 実測）。隣の欄の矩形を渡せばはみ出しを完全に 0 にできる仕組みも入れたが、読み取り本体からの受け渡しは未接続（別担当の作業と重なるため）
- **読み取りの可視化で、値の出どころを2箇所で別々に決めていた（#65-6）**: `debug-images` が描く「この欄は参照先から採った」の印は、保存済みの結果ではなく文字の座標から計算し直していた。同じ結論になるよう書かれてはいたが、判断の規則が読み取り本体と可視化の2箇所に並ぶため、片方だけ直せば画像と実際の出力が食い違う。中間データに保存されている値をそのまま描くようにした。あわせて「主の欄と参照先の両方に書かれていて食い違う」欄（信頼度に関わらず〓になる）を、専用の枠の色と凡例で見分けられるようにした
- **最低信頼度の集計で丸めた値と丸めない値が混ざっていた（#65-10）**: 1行の「最低信頼度」を出すとき、通常の欄は丸める前の値を、文字単位で〓にした欄は3桁に丸めた値を同じ集計に入れていた。丸める場所を1箇所にまとめた。出力される文字列は変わらない（golden 比較でバイト一致）。判定に使う値そのものは丸めない——3桁に丸めると閾値ちょうど付近で〓の判定が反転するため
- **管理列の数を直書きしていた箇所を残していた（#53 L-8）**: 出力の書き出しで、抽出対象の列数を求めるのに `6` を直接書いている行が1つ残っていた。列名の一覧を持つ定数から数えるようにした（出力は変わらない）
- **割付の索引をページごとに作り直していた（#53 L-18）**: 文字をどの欄に入れるかを引くための索引は、テンプレートが変わらない限り中身も変わらないのに、1ページ処理するたびに全欄を走査して組み直していた。同じテンプレートなら使い回す。出荷テンプレート（194 欄）で、この処理は 8.29ms → 4.13ms/ページ（100 回の平均・2026-09-03 実測）。ページ数の少ない実行では全体の所要時間に対して誤差の範囲で、効くのは枚数が多いときだけ

- **割付のやり直し（`remap`）が二重実行の歯止めを通っていなかった（#93）**: 読み取り（`run`）と再出力（`render`）は同じ作業フォルダでの二重起動を断るが、`remap` だけ断らずに共有データを書き換えていた。さらに CLI の `remap` は「割付のやり直し → 出力し直し」の2段で、その間に別の実行が割り込む隙間が常にあった。`remap` にも同じ歯止めを付け、2段を通して隙間なく実行するようにした。あわせて1ページ分の書き込み（読み取った文字・欄の値・元号のスコア・使ったテンプレートの印）をひとまとまりで確定させ、途中で落ちても「欄の値は新しいのに、どのテンプレートで割り付けたかの印は古い」という食い違いが残らないようにした（この食い違いが残ると、次の出力が「テンプレートが変わっている」と誤って拒否される）
- **差し替えた紙の帳票 ID が不必要に変わっていた（#53 L-5）**: 同じ名前で中身の違うファイルに差し替えて読み取り直すと、旧データは消えているのに使用済み ID の一覧が更新されず、帳票 ID が `<ファイル名>_<8桁の英数字>_p0001` の形へ逃げていた。元の `<ファイル名>_p0001` をそのまま使う
- **今回の読み取りが全滅しても成功として終わることがあった（#53 L-9）**: 終了コードの判定が、作業フォルダ全体の出力行数と今回の失敗数を比べていたため、過去に読み取った行が1行でも残っていれば、今回入れた紙が1枚残らず失敗しても「成功」で終わっていた（自動実行から成否を判定できない）。今回処理した紙だけで判定する。送信の上限に達して見送ったぶんは失敗に数えない（分割して送るのは通常の使い方のため）
- **欄が1つも無いテンプレートで読み取ると原因不明の「様式不一致」になっていた（#53 L-10）**: 中間データの書き込みが、残す欄が0件のときに成立しない SQL を組み立てて落ち、その失敗が「様式不一致」に化けていた。0件のときはそのページの欄をすべて消す動きに直した
- **文字ごとの信頼度・値の由来が黙って書き込まれないことがあった（#65-9）**: これらは既存の欄の行を更新する形で保存しており、対象の行が無くても成功として通っていた（更新0件でも UPDATE は成功するため）。値だけが既定のまま残り、文字単位の〓も由来の色も出ないのに出力自体は作られるので気づけない。更新できた件数を確かめ、合わなければその場で止める
- **配布版を画面を使わずに直接実行したときの、使うテンプレートの食い違い（#65-4）**: 配布した実行ファイルをテンプレートの指定なしで直接叩くと、画面から起動したときとは別の場所のテンプレートを読む余地があった（画面は前回使ったテンプレートを毎回明示して渡している）。前回が利用者テンプレートだった場合は、その保存場所は画面側しか知らないので、黙って同梱のテンプレートで処理せず「テンプレートのパスを指定してください」と止める。前回が同梱のテンプレートなら従来どおり（同じ実体を読む）。開発中の実行（`python -m ...`）の動きは変えていない
- **記載の訂正: 中間データが消えるのは `purge` だけではない**。同じ名前の入力ファイルを中身の違うものへ差し替えて `run` すると、そのファイルの旧ページと派生データ（token / cell / alignment / era_score）を消してから読み直す（#43 H-B。旧値が「正常」の顔で出力に残り続けるのを防ぐため）。0.1.0 の Security 節「削除は明示操作（`purge --yes`）のみ」と README の `purge` 節はこの経路を書いていなかったので、README 側に追記した。挙動そのものは変えていない (#53 L-3)
- 読み取りを続けて実行すると、前回の完了サマリが今回の実行の表示を上書きすることがあった。コアの終了だけを見て次へ進んでいたため、出力を読み取る側がまだ読み終えていない行を取りこぼし、次の実行が始まった後に前回の行が届いていた。読み取りが終わるまで待ってから戻すようにし、あわせて進捗の各行に実行の識別子を付けて、画面側が前回のぶんを捨てられるようにした。発生は数ミリ秒の窓で手の操作では稀だが、連続実行や自動テストでは表面化する (#96)
- GUI の画面テスト（Playwright）が、他の作業と並行して回すときだけ稀に時間切れで失敗していた。画面の描き直しを固定時間の待ちで当て込んでいたため、機械が混んでいると間に合わなかった。待ちを「その状態になるまで待つ」形へ置き換え、待ち時間の上限も 1 か所に集約した。ネットワーク共有のパス名を実際に解決していたテストも、解決を差し替えて共有の有無に左右されないようにした (#79)
- ファイル・フォルダの選択ダイアログを開いている間、GUI 全体が止まっていた。ダイアログをそのまま閉じずに放置すると他の操作も進まず、閉じるまで固まったように見える（2026-09-02 の実機確認では 2 分以上）。ダイアログの表示を別スレッドへ移し、開いている間も画面の他の処理が動くようにした。選択の結果と扱い（読み書きを許可するファイルの登録範囲）は変えていない (#84)
- 利用者テンプレートの保存が、保存先や一時ファイルの位置に置かれたリンク（シンボリックリンク・ジャンクション）をたどって、テンプレート保存フォルダの外へ書き込む余地があった。一覧表示と読み込みは以前からリンクを除外していたが、保存だけ検査が抜けていた。書き込みの前にリンクを拒否し、書いた後にも実体がテンプレート保存フォルダの直下にあることを確かめて、外れていれば書いたファイルを消して保存を失敗させる。あわせて、大文字小文字の畳み込みの違い（Windows のファイル名の扱いと Unicode の小文字化は一致しない）で「新規保存」と判定した先に実ファイルがあった場合も、上書き確認なしでは保存しないようにした (#89, #90)
- 設定（config.json）の書き込みが途中で切れると設定ファイルが壊れ、壊れた設定への上書きを拒否する防御と噛み合って GUI から二度と保存できなくなっていた（手で config.json を消すまで復旧できない）。一時ファイルへ書いてから差し替える方式に変え、失敗しても元の設定をそのまま残す。設定の保存はテンプレートを切り替えるたびに走るため、中断に当たる機会は少なくない (#97)
- 認証キー（サービスアカウントの JSON）の取り込みが、JSON として読めさえすれば何でも通していた。`{}` や配列でも取り込めてしまい、誤りに気づくのは帳票を投入して読み取りを始めた後——最も手戻りの大きい場面だった。取り込むときに「サービスアカウントの鍵か」（`type`・`client_email`・`private_key`）を確かめ、通らなければ取り込まずに理由を出す（鍵の中身とファイルの場所はメッセージに出さない）。取り込み済みの鍵が壊れている（復号できない・形が違う）場合も `verify` は緑にならず、取り込み直しを案内する。鍵ファイルの書き込みは一時ファイルからの差し替えに変え、書き込み中に中断しても前の鍵を壊さない (#97)
- config.json の書き方が壊れていると、コアが「詳細は error.log を参照」とだけ言って止まっていた。この失敗はログを開く前に起きるので error.log には何も書かれておらず、案内先が存在しなかった。何行目のどこが読めないのかと、直せないときは config.json を消せば既定値で起動することを、その場に出す (#97)
- API 送信数の月次カウンタが壊れていると、何も言わずに 0 から数え直していた。上限（既定 900）の保護が 1 か月分まるごと外れても気づく手段がなかった。壊れたファイルを `api_usage.broken.<日時>.json` へ退避してログに残す。0 から数え直す動作自体は変えない——カウンタ 1 つで読み取りを止めるほうが害が大きい。あわせて一時ファイルの名前にプロセス番号を入れ、別々の作業フォルダで同時に動かしたときに同じ一時ファイルを奪い合って壊れる経路を塞いだ。※読む→数える→書く の間に他のプロセスを締め出す仕組みは入れていない（同時実行 1 回につき最大 1 ユニットの数え落ち。1 つの入力フォルダを 1 人が処理する想定運用では起きにくい） (#91)
- Vision API の応答を保存した直後、状態を「受信済み」へ書き換える前に落ちると、次の読み取りで同じページをもう一度送っていた（＝二重課金）。応答ファイルの書き込みを一時ファイルからの差し替えに変え、読み取りの開始時に「応答は残っているのに状態が古い」ページを見つけて、送り直さずに状態だけ進める（進めた件数は完了サマリの `recovered_responses` に出る）。応答には送った画像のハッシュを添えて保存し、再利用するときに今の画像と食い違えば使わずに送り直す。ハッシュが付いていない過去の応答は従来どおり再利用する (#92)
- Vision API が「権限がない」「引数が不正」のような、何度送っても同じ結果になるエラーを返しても、ほかのエラーと同じく 5 回まで送り直していた。ページ 1 枚につき月次カウンタを 5 ユニット消費し、約 15 秒待たされた末に同じエラーが返る。応答のエラー番号（gRPC ステータス）を見て、決定的なもの（3・5・7・9・12・16）は 1 回で止める。通信エラーやサーバー内部エラーのような一時的な失敗は従来どおり 5 回まで再試行する (#99)
- テンプレート照合が失敗したとき、コアのエラー出力がそのまま画面へ出ており、設定値（作業フォルダなど）の絶対パスが混じる余地があった。テンプレート保存の検証と同じく、決めたキーだけを通す方式へ揃えた（失敗の理由は固定のコードだけを返す）。照合に渡せるテンプレートの件数にも上限（一覧と同じ 20 件）を設け、超えた場合は黙って切らずに理由を返す (#90)
- `purge --yes` が workdir をディレクトリごと消していたため、取り込み済みの資格情報 `cred.dpapi` まで一緒に消え、次の読み取りで `import-credentials` のやり直しが必要になっていた。README の「消えるのは中間データだけ」とも食い違っていた。残すものを資格情報 1 件に限定し、それ以外は種類を問わず全部消す方式（keep-list・個人情報に対して fail-closed）へ変え、workdir のフォルダ自体は空のまま残す。あわせて、1 件が消せなくても（使用中・読み取り専用）残りを続行して削除件数と失敗件数を`purged` イベントと画面の 1 行に出し、失敗があれば終了コード 1 にする（読み取り専用ファイルは書き込み許可を戻して一度だけ再試行）。workdir 自体が symlink・ジャンクションなら何も消さずに終了し、配下のリンクはリンク先を辿らずリンクだけ外す（旧実装の `rmtree` はリンク先の中身を消さなかったが、新方式で `iterdir` がリンク先を辿る後退を防ぐため）。`workdir/golden/`・`workdir/s2/` のような開発素材も従来どおり消える——置き場の分離は #88 (#83)
- テンプレート編集で PDF を開くと `unrecognized arguments: --no-mask` で失敗していた。原因は同梱 exe（`core-dist/`・2026-08-31 16:45 ビルド）が再ビルドされないまま、開発起動の GUI にも無条件に優先されていたこと。ソースに欠陥はなく、回帰ゲートも同梱物を見ていなかったため 17 commit のあいだ気づけなかった。開発チェックアウト（`.git` と `.venv` がある）では GUI が `.venv` のコアを起動するよう変え（`CHOUHYO_CORE=bundled` / `=venv` で強制できる。前後の空白と大文字小文字は無視し、それ以外の値は起動エラーにして黙って自動判定へ落とさない）、`build_dist.py` が書く `BUILD_STAMP.json`（core/・schema/・出荷テンプレートの内容ハッシュ）を `run_all_tests.py` が現在のソースと比べて、同梱 exe がソースより古ければゲートを FAIL にする（`core-dist/` が無い環境は SKIP）
- 編集画面で画像の範囲外へ出た欄・表・除外領域が、保存時に無言で消えていた。範囲外の要素があるときは一時ファイルへ書く前に保存を中止し、対象の id を列挙する。最も重いのは除外領域の欠落——マスクが外れたまま記入内容が Vision へ送られる (#69 Q-H2)
- 実行タブを表示している間も編集画面のキー操作が生きていた。Delete で選択中の欄・表・除外領域が確認なしに消え、フォーカス中のボタンで Space が奪われる (#69 Q-H3)
- 縦横比の違う入力画像を検証なしでテンプレート寸法へ引き伸ばしていた。着手前の実測では、縦だけ 5% 伸ばした入力が例外なく通り、位置合わせも `angle=0 / dx=0 / dy=0` で「成功」と判定していた（歪んだ内容が正常な顔で送信される）。アスペクト比の相対差が 1% を超えるページは様式不一致として送信前に止める (#69 Q-H1)
- 全ページが様式不一致で1行も読めなくても `exit 0` を返していた。終了コードの判定を `align_failed` ＋ `format_mismatch` にした (#69 Q-H1)
- `verify` を実行できなかったときに、既定値（API 残量 900 ユニット・Poppler の状態）を現状として画面に出していた。「verify の行を1つも受け取らなかった」を区別し、再試行の導線を出して開始ボタンを無効化する (#69 Q-ME)
- 設定の読み込みに失敗しても画面は既定値を表示し、そのまま1項目を保存すると 6 項目すべてを既定値で上書きできた。読込失敗時は入力と保存を無効化して理由を出す (#69 Q-MF)
- `run_core` の PID スロットが早期 return の経路で解放されず、以後の実行が「すでに実行中」で恒久的にブロックされた。Drop で解放し、異常系だけ子プロセスを止める (#69 Q-MB)
- `Store` の close が例外経路で漏れていた。生成5箇所を `with` へ統一し、正常・例外のどちらでも close が1回だけ呼ばれることをテストで固定した (#69 Q-MG, #53 L-7)
- 行の値数が導出列数と食い違っていても .xlsx / .csv を書けていた。書き込み前に拒否する（メッセージに出すのは page_id だけで記入値は出さない） (#69 Q-MH)
- 列数比較の fail-open: 比較基準が取れていない状態（verify 失敗・応答欠落）では、列が減る保存でも無警告で通っていた。基準・現在値のどちらが欠けても「列数を比較できません」を保存前確認に出し、列数不明時は保存後メッセージにも数値を出さない (#65)
- 「テンプレートが拒否されないこと」しか確認していなかったテストを、列数ゲートの実検証（一致・不足・省略の3方向）へ置き換えた。警告を外すとテストが赤になることをミューテーションで確認済み (#65)
- 保存時の差分表示が無編集の保存でも「欄 14 → 194（+180）」と出ていた。読み込み時と保存時で数える母集団が違ったのが原因で、比較基準を verify 応答へ一本化した (#66)
- 主の欄が非空のとき、参照先に書かれた文字が無言で消えていた。受け皿を失った欄は〓にする (#54)
- 切り抜きの穴に落ちた文字が、部分的な値のまま高信頼として出ていた (#59 H-4)
- `resolveOverlaps` が消えた旧領域の取り分を保持し続け、無関係な第三の欄を削っていた (#59 H-3)
- `autoCarve` の後に選択添字がずれ、別の欄が選ばれた状態になっていた (#60 M-4)
- 表の choice 列の幅を変えても選択肢マークが追従せず、コアが読み込みを拒否するテンプレートを作れた (#60 M-8)
- 切り抜き直後の Ctrl+Z が効かず、画面の案内と挙動が食い違っていた (#61 L-5)
- `source_renamed` / `rename_fallback` が画面に出ず、課金の動く分岐が見えなかった (#60 M-2)
- `editor_pages` に位置合わせ画像（`-aligned.png`）が消えずに残っていた (#60 M-7)
- テンプレートが壊れているときに「枠は動かさないでください」という誤った案内が出ていた (#60 追補)
- `debug-images` の〓判定が本出力とずれ、破棄した参照先を採用色で塗っていた (#60 M-1②③)
- 参照先どうしが重なったときのエラー文が、実際の対象を指していなかった (#61 L-3)

- 出力列の並べ替え（[↑][↓]）が、行の点滅だけで結果を返していた。読み上げソフトを使う人には何が起きたか伝わらず、面の先頭や末尾まで動かすと押したボタンが押せなくなってフォーカスの行き先が無くなっていた。移動した結果（「氏名 を 2 番目に移動しました（全 5 件中）」）を読み上げ、押せなくなる側から反対側のボタンへフォーカスを移す。表の列では押したボタンではなく移動先の行のボタンへ移す——以前は入れ替わった相手の列のボタンにフォーカスが残り、続けて押すと動かしているつもりの列がずれていった (#67)
- 保存前の確認で「このまま保存」を押した後、2つのボタンが押せなくなるだけでラベルも説明も変わらず、処理中なのか押し漏れたのか分からなかった。処理中はラベルを「保存しています…」に変え、押せない理由を添える (#67)
- 保存のときに重なった枠を自動で切り抜いた分が、元に戻す（Ctrl+Z）の履歴に入っていなかった。検証で保存が止まると切り抜きだけが画面に残り、戻す手段が無かった。切り抜きを1コマとして履歴へ積む (#65-8)
- 枠候補を作ったときの「候補にしなかった枠」の内訳で、四方が線でつながっていない枠だけが英字のコードのまま出ていた。日本語（四方が線でつながっていない）で出す (#85)

### Security

- **鍵の取り込みで元ファイルの削除を既定にした（GUI 側・#52 M-10）**: 実行画面の「認証キーを選択」は、選んだ後に確認を出してから `--delete-source` 付きで取り込む。確認には「元のファイルは取り込み後に削除します（鍵が平文のまま残らないように）」と書く。取り込み後のメッセージは結果で変わる——削除できたら「元のファイルを削除しました」、できなかったら「手で削除してください（鍵が平文のまま残っています）」。削除の可否を伝えるイベントが無い版のコアと組み合わさったときは、消えていないと見なした文言へ倒す（消えていないのに「削除しました」とは言わない）
- **鍵を選ぶダイアログの結果を、読み書き許可リストへ入れないことを用途側で固定した（#69 セキュリティ LOW (c)）**: ファイル選択ダイアログの結果は既定で「以後この場所を読み書きしてよい」リストへ入る（テンプレート編集が任意の場所の JSON を開くため）。認証キーの取り込みは呼び出し側でこれを切っていたが、切り忘れると鍵の平文 JSON がセッション中ずっと読める状態になる。用途が「認証キー」のときは呼び出し側の指定によらず登録しないようにした
- **ドラッグ＆ドロップの許可登録を、受け取る画面が見えているときだけにした（#69 セキュリティ LOW (b)）**: ファイルをウィンドウへ落とすと、そのパスが読み書き許可リストへ入る。この登録はタブと無関係に動いていたため、テンプレート編集タブを見ている最中に落としたファイルも——画面には何も出ないまま——許可リストに増えていた。実行画面が表示されている間だけ登録する
- **一時保存ファイルを「名前を外してから新規作成」で書くようにした（#69 セキュリティ LOW (a)）**: テンプレート保存の一時ファイル（`<名前>.json.saving.json`）の位置に、範囲外のファイルへのハードリンク（同じ実体への別名）を先回りで置かれると、上書きがそのリンク先へ届く。既存のリンク検査はシンボリックリンクとジャンクションしか見ておらず、ハードリンクは素通りしていた。リンク数を読む標準の API は安定版の Rust では使えない（`windows_by_handle`・rustc 1.94.1 で確認）ため、名前を先に外してから新規作成でのみ書く方式にした——ハードリンクの名前を1つ消してもリンク先の中身は消えず、書く先は必ず新しい実体になる
- **エクスプローラと taskkill を絶対パスで起動するようにした（#53 L-12）**: 「フォルダを開く」と「中断」が名前だけで実行ファイルを起動しており、探索経路に同名の実行ファイルを置かれると乗っ取られる余地があった（探索順に書き込み可能な場所が含まれるかは未検証）。`%SystemRoot%` を起点にした絶対パスで起動する。環境変数が無いときだけ従来どおり名前で起動する
- **取り込んだ認証キーを作業フォルダの外へ移した（#52 M-11）**: `cred.dpapi`（暗号化した認証キー）は作業フォルダの直下にあり、そこは中間データを消すコマンド `purge` の対象でもあった。「読み取ったデータを消す」という正しい操作が認証キーを巻き込むため、キーを消したくない利用者が手元に平文の鍵ファイルを持ち続ける動機になっていた。置き場を `%LOCALAPPDATA%\ChouhyoOCR\` に変え、中間データとキーの寿命を切り離した。以前の版で取り込んだキーは、次に読んだときに自動で新しい置き場へ移る（取り込み直しは不要）。`verify` が返す状態表示（`dpapi` / `env` / `missing`）は変えていない
- **鍵の取り込みで元の平文ファイルを消せるようにした（#52 M-10）**: `import-credentials <json> --delete-source` を付けると、暗号化した保存に成功した後で、元の JSON をランダムなデータで3回上書きしてから削除する。付けないときの案内文も「削除して構いません」から「削除してください（鍵が平文のまま残ります）」に変えた——取り込みのたびに有効な秘密鍵が平文でディスクに残る状態を作り直していたため。削除できなかったときは「完了」で終わらせず警告を出す（残っているファイルの場所は画面にもログにも出さない）
- **課金カウンタの差し替え用の環境変数を、名前でテスト専用と分かるようにした（#52 M-6）**: 月次の送信上限を数えるファイルの場所は `CHOUHYO_USAGE_DIR` という環境変数で変えられたが、名前からも文書からも用途が読めず、上限を回避する経路として使えてしまう状態だった。`CHOUHYO_USAGE_DIR_FOR_TESTS` に改名し、docstring と README に「テスト専用・運用では場所を変えられない」と明記した。カウンタが壊れていたときの扱い（別名へ退避してログに残し、0 から数え直して処理は続ける）は #91 の判断のまま変えず、README にその理由を書いた。ファイルを消せば 0 に戻る点も仕様として残す——このカウンタが止めたいのは気づかないうちの送りすぎで、利用者が自分で外す操作までは防がない
- **認証キーの復号に失敗した理由を言い分けるようにした（#53 L-15）**: どの失敗でも「別のアカウントで実行していないか確認する」の1種類だけを出していたため、暗号化ファイルが壊れている場合（実測: Windows エラー 87）にアカウントを確認しても直らなかった。壊れている系のエラーコードでは「取り込み直す」と案内する

- **鍵の検出を「項目名」から「中身の量」へ（#98 の作業中に判明）**: `.githooks/pre-commit` の内容検査が項目名（`service_account` 型の宣言・`private_key` フィールド・PEM のヘッダ行）だけを見ていたため、資格情報を扱うソースとそのテスト（`core/chouhyo_ocr/cred_store.py`・`core/tests/test_credentials_warn.py`・`core/tests/test_cred_config_atomicity.py`）を直すたびに commit が止まり、`--no-verify` を促す形になっていた。フックを外させる検査は防御にならない。本物の認証キーだけが持つ長さで見る 5 本へ差し替えた——200 文字超の `private_key` の値／20 桁以上の16進の `private_key_id`／60 文字以上の base64 だけの行（行頭の字下げと行末の CR を許す）／PEM の先頭行の直後に 100 文字以上が続く形／Google API キー。**行末アンカーの手前に空白を許さないと CRLF のリポジトリで PEM が丸ごと素通りし、ヘッダ行だけを見ると改行を詰めた PEM・`.env` 形式・ソースへの `
` 埋め込み代入を取りこぼす**（合成鍵 28 形態で実測）。あわせて「自分自身を検査対象から外す」除外を廃止した（除外はそのまま穴になる。パターンは自己一致しない綴りにしてある）。実測（2026-09-03）: 追跡 169＋未追跡 16 ファイルで誤検出 0 件、退行 5 形態（`.env` 形式・改行落とし・ソース内の `
` 埋め込み・字下げ・CRLF）をすべて検出。同じ 5 本を `.github/workflows/guard.yml` にも置いた
- **検査の失敗を「該当なし」にしない（fail-closed）**: `git ... | tr` の終了コードはパイプ末尾の `tr` のもので、git が落ちても（壊れた正規表現・index 破損）常に 0 が返り、検出 0 件と区別が付かなかった。旧実装は壊れた正規表現を渡しても exit 0 で通っていた（実測）。git を単独で走らせて終了コードを見る形に変え、失敗したら理由を出して commit を止める。ワークフロー側は `set -euf -o pipefail`
- **JSON をパス基準に追加**: 認証キーは JSON で配られる。`.gitignore` が全階層の `*.json` を落として追跡中のものだけ否定行で戻す流儀なので、フックとワークフローにも同じ許可リストを置いた。免除は完全パスか実在ディレクトリ限定にする——広い glob にすると、そこへ置くだけで検査を外せる
- **`.gitignore` の testdata 例外を実在パスの列挙へ**: `!/testdata/**/*.json` は testdata 配下の任意の JSON を追跡対象へ戻してしまい、`testdata/formB/responses/本番読取_p0001.json`（実データの Vision 応答）がNOT IGNORED になっていた（実測）。戻すのは現に追跡している 4 ファイルだけにした
- **改名した帳票 Excel / CSV の混入を塞いだ（#98）**: `.gitignore` と `.githooks/pre-commit` の両方が `output_` で始まる名前しか見ておらず、納品用に `customer.xlsx` へ改名しただけで2層とも素通りした（実測: `results/final.csv`・`docs/顧客一覧.xlsx`・`x.xlsx.tmp` も同様）。除外を拡張子ベースに変え、`*.xlsx` / `*.xls` / `*.xlsm` / `*.csv` と、併記される列名一覧（`*_columns.txt`）・書き換え途中の一時ファイル（`*.tmp`）を全階層で落とす。フック側にも同じ分岐を足した——xlsx は zip なので内容検査が届かず、パスで止めるのが唯一の関門になる。追跡中のこの拡張子のファイルは 0 件のため戻す否定行は要らない
- **ログからテンプレートファイル名・欄名を除く（#77）**: テンプレート名・欄名に顧客名などが入る想定（Q-S1）を受け、秘匿対象を記入値からテンプレートファイル名・欄名（field_id・列名・table_id）へ広げた。ログには `template_hash` と序数（`cell_idx`・`col_idx`・`face_idx`）だけを残し、復号手順を `logging_safe.py` に記載。白リスト外のキーで無言に落ちていた W-3/W-4 の診断ログを復活させ、AST の静的検査でキーの取りこぼしを検出する。stdout の JSON Lines（GUI 契約）と出力ファイルは従来どおり
- **`write_config` の値検証**: 既知8キー以外の書き込みを拒否し、`output_dir` / `workdir` / `log_dir` は空・ドライブ直下・UNC・`..` を弾く。検証していないパスがそのまま許可ルートになると、GUI の画像読み取りの範囲がボリューム全体まで広がる。手で書き換えられた `config.json` も想定し、許可ルート側でも同じ検査をする (#69 Q-MC, S-MA)
- **GUI から渡す CLI 引数のスコープ検査**: `run --input` の値がパススコープ検査を通っておらず、利用者が選んでいないフォルダの PDF を Vision へ送れた。サブコマンド別のフラグ表で許可外のフラグ（`--replay` / `--resend-on-template-change`）を GUI 境界から禁止し、`--input` はピック済みパスの配下だけを通す (#69 S-MD, #52 M-7)
- **平文鍵を常時警告**: 環境変数 `GOOGLE_APPLICATION_CREDENTIALS` の平文鍵で動いていても `verify` が緑判定だった。`warn: true`（`reason: env_plaintext`）を返し、GUI の実行画面は取り込み導線つきの警告を出す（取り込むまで消えない）。実行自体は止めない (#69 S-MB)
- **出力ファイルの削除手段（`purge --include-output`）**: 読み取った記入値がそのまま並ぶ .xlsx / .csv / `_columns.txt` に削除手段がなく、`purge` の対象外だった。生成物の命名（`output_<日時>` 系と `.bak` `.tmp`）に一致するファイルだけを1件ずつ消し、フォルダと命名外のファイルは残す。削除件数・残した件数・削除できなかった件数をログと標準出力に出す (#69 S-MC, #52 M-11)
- **`pdftoppm` / `pdfinfo` へ渡すパスの先頭 `-` を `./` 前置で無害化**: ファイル名がオプションとして解釈される余地を塞いだ（CWE-88） (#69 L-S2)
- **`.gitignore` の testdata 例外を `*.json` / `*.png` に限定**: `!testdata/**` が testdata 配下の全除外を無効化しており、帳票 PDF や SQLite が例外の穴から入りうる状態だった。`git check-ignore` の終了コードで21パスを再検証（回帰0） (#69 S-H1)
- **除外領域の後退を保存前に検知**: 件数の減少・同一 id の座標やサイズの変化（135px ズレ型）・id の入れ替わりを確認対象とする。除外が失われたテンプレートが実際に9件生成されていた事故への恒久対策 (#55)
- **`debug-images --out` に同期フォルダ検査**: 記入内容の写った画像がクラウド同期先へ出るのを拒否する (#59 H-5)
- `read_default_template` のエラーメッセージから絶対パスと OS の詳細を除いた (#61 L-2)

## [0.1.0] - 2026-08-27

初回リリース。要件定義（v3.11）・設計（v2.0）・実装・配布物までを単一サイクルで構築した。

### Added

- **Python コア（CLI）**: `run` / `render` / `remap` / `detect-grid` / `status` / `verify` / `import-credentials` / `purge`。処理は 取り込み→表裏分割→面ごと位置合わせ→Vision 送信→symbol 単位割付→丸印判定→218列出力
- **〓運用**: 低信頼セルは値を出さず〓（背景色つき）。行頭の要確認セル数（COUNTIF）で目検を優先順位づけ。閾値変更後は `render` だけで再出力（API 再送なし）
- **再開**: SQLite（WAL・UPSERT）に進捗と読取値を永続化。中断・強制終了後も処理済みの紙を再送信しない
- **元号丸印判定**: 印字「昭・平・令」の左右帯インク比＋共通フロア減算。自作サンプル実測で判定 5/5 正解
  ※後の較正で、この「5/5」は誤った正解表に対する 5/8 だったと判明した（丸が印字文字へきつく重なった 3 箇所を正解表が「丸なし」としており、旧実装の取りこぼしが見えていなかった）。正しい正解表と較正の経緯は `core/tests/test_era_calibration.py` を参照 (#53 L-2)
- **複合セル分割**: 家族欄の「7.7.20」を年/月/日へ機械分割（分割数不一致は全サブ列〓）
- **枠候補の自動生成**: 罫線の射影検出（実測残差 2px）と等分割生成。テンプレート編集画面から利用
- **Tauri GUI（2画面）**: 初学者向けの番号つき手順の実行画面（進捗・中断・完了サマリ6項目・失敗一覧・次にやること案内・鍵の初回取り込み・設定6項目）と、管理者向けテンプレート編集画面（ズーム/パン・枠ドラッグ・表の自動生成・保存時コア検証）
- **配布**: PyInstaller onedir のコア＋Poppler＋スキーマ同梱、NSIS インストーラ（署名なし）
- **資格情報保護**: サービスアカウント JSON を DPAPI で暗号化保存（平文を残さない）
- **テスト 91件**: 純関数（列導出・正規化・〓判定）／保存済み Vision 応答による実データ回帰／`render` 再現性のバイト一致／送信上限・再開／実プロセス強制終了→再開／GUI スモーク（Playwright・デモモック）

### Security

- Vision へ送る画像から除外領域（綴じ穴・黒塗り・印字ラベル）をマスク
- ログ・進捗イベントに帳票の記入値を書かない（許可キー白リスト方式）
- 中間データはローカル完結・削除は明示操作（`purge --yes`）のみ

[Unreleased]: https://github.com/abroadcrew02-spec/Chohyo/compare/v0.1.0...HEAD
[0.1.0]: https://github.com/abroadcrew02-spec/Chohyo/releases/tag/v0.1.0
